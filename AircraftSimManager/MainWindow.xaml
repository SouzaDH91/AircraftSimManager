<Window x:Class="AircraftSimManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AircraftSimManager.ViewModels"
        xmlns:views="clr-namespace:AircraftSimManager.Views"
        xmlns:helpers="clr-namespace:AircraftSimManager.Helpers"
        mc:Ignorable="d"
        Title="Aircraft Sim Manager" Height="1000" Width="1200"
        WindowStartupLocation="CenterScreen" Background="#1E1E1E">

    <Window.Resources>
        <!-- DataTemplates de Navegação -->
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LiveryManagerViewModel}">
            <views:LiveryManagerView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:IncomingViewModel}">
            <views:IncomingView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:FuelCalculatorViewModel}">
            <views:FuelCalculatorView />
        </DataTemplate>

        <!-- Separador Dark -->
        <Style TargetType="{x:Type Separator}">
            <Setter Property="Background" Value="#3F3F46"/>
            <Setter Property="Margin" Value="4,3"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Separator}">
                        <Border Height="1" Background="{TemplateBinding Background}" Margin="{TemplateBinding Margin}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 1. ESTILO PARA O MENU PRINCIPAL (TOPO) - Texto Centralizado, Sem coluna de checkmark -->
        <Style x:Key="TopLevelMenuItemStyle" TargetType="{x:Type MenuItem}">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter ContentSource="Header" 
                                                  RecognizesAccessKey="True" 
                                                  VerticalAlignment="Center" 
                                                  HorizontalAlignment="Center"/>

                                <Popup x:Name="PART_Popup" 
                                       AllowsTransparency="True" 
                                       Focusable="False" 
                                       IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" 
                                       Placement="Bottom" 
                                       PopupAnimation="Fade">
                                    <Border Background="#252526" 
                                            BorderBrush="#3F3F46" 
                                            BorderThickness="1" 
                                            CornerRadius="3" 
                                            Padding="2" 
                                            Margin="0,2,0,0">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Direction="270" Opacity="0.4" Color="Black"/>
                                        </Border.Effect>
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsSubmenuOpen" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 2. ESTILO PARA OS SUBMENUS - Alinhado à Esquerda com Espaço do Checkmark (✓) -->
        <Style x:Key="SubMenuItemStyle" TargetType="{x:Type MenuItem}">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="22"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock x:Name="CheckMark" 
                                           Text="✓" 
                                           Foreground="#007ACC" 
                                           FontWeight="Bold" 
                                           FontSize="13" 
                                           Visibility="Collapsed" 
                                           VerticalAlignment="Center" 
                                           HorizontalAlignment="Center" 
                                           Grid.Column="0"/>

                                <ContentPresenter ContentSource="Header" 
                                                  RecognizesAccessKey="True" 
                                                  VerticalAlignment="Center" 
                                                  HorizontalAlignment="Left" 
                                                  Grid.Column="1"/>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Menu Superior -->
        <Menu DockPanel.Dock="Top" Background="#252526" Foreground="White" FontSize="14" Padding="5">

            <MenuItem Header="{helpers:Loc Home}" 
                      Style="{StaticResource TopLevelMenuItemStyle}"
                      Command="{Binding NavigateToDashboardCommand}" />

            <MenuItem Header="{helpers:Loc Tools}"
                      Style="{StaticResource TopLevelMenuItemStyle}">
                <MenuItem Header="{helpers:Loc LiveriesManager}" 
                          Style="{StaticResource SubMenuItemStyle}"
                          Command="{Binding NavigateToLiveryManagerCommand}" />
                <MenuItem Header="{helpers:Loc FuelCalculatorTitle}" 
                          Style="{StaticResource SubMenuItemStyle}"
                          Command="{Binding NavigateToFuelCalculatorCommand}" />
                <MenuItem Header="{helpers:Loc PerformanceCalculator}" 
                          Style="{StaticResource SubMenuItemStyle}"
                          Command="{Binding NavigateToPerformanceCalculatorCommand}" />
            </MenuItem>

            <!-- Menu de Idiomas -->
            <MenuItem Header="{helpers:Loc Language}" 
                      Style="{StaticResource TopLevelMenuItemStyle}">
                <MenuItem 
                    Header="{helpers:Loc English}" 
                    Style="{StaticResource SubMenuItemStyle}"
                    IsCheckable="True"
                    IsChecked="{Binding IsEnglish, Mode=OneWay}"
                    Command="{Binding ChangeLanguageCommand}" CommandParameter="en-US"/>
                <MenuItem 
                    Header="{helpers:Loc PortugueseBr}" 
                    Style="{StaticResource SubMenuItemStyle}"
                    IsCheckable="True"
                    IsChecked="{Binding IsPortuguese, Mode=OneWay}"
                    Command="{Binding ChangeLanguageCommand}" CommandParameter="pt-BR"/>
            </MenuItem>
        </Menu>

        <!-- Área Central -->
        <ContentControl Content="{Binding CurrentView}" Margin="10" />
    </DockPanel>
</Window>